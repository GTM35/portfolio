import styled from "styled-components";
import { headingXXL } from "../../assets/styles/global";

export const ContainerProject = styled.div`
  margin-bottom: 500px;
  display: flex;
  gap: 32px;
  flex-wrap: wrap;
`;

export const ProjectsTitle = styled(headingXXL)`
  font-weight: bold;
  margin-bottom: 48px;
`;
